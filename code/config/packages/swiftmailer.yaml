swiftmailer:
    transport:        gmail
    username:         '%env(MAILER_USERNAME)%'
    password:         '%env(MAILER_PASSWORD)%'
    host:             localhost
    port:             465
    encryption:       ssl